for this workshop, we used amazon elastic beanstalk, zipped up the entire project using VS code's winarchieve extension (must be used), and sent the file to an amazon host. 

Note: NEVER push ur API key to git. This is just an example. 